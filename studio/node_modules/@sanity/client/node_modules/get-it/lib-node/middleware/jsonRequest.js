"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _isPlainObject = require("is-plain-object");

const serializeTypes = ['boolean', 'string', 'number'];

const isBuffer = obj => !!obj.constructor && typeof obj.constructor.isBuffer === 'function' && obj.constructor.isBuffer(obj);

var _default = () => ({
  processOptions: options => {
    const body = options.body;

    if (!body) {
      return options;
    }

    const isStream = typeof body.pipe === 'function';
    const shouldSerialize = !isStream && !isBuffer(body) && (serializeTypes.indexOf(typeof body) !== -1 || Array.isArray(body) || (0, _isPlainObject.isPlainObject)(body));

    if (!shouldSerialize) {
      return options;
    }

    return Object.assign({}, options, {
      body: JSON.stringify(options.body),
      headers: Object.assign({}, options.headers, {
        'Content-Type': 'application/json'
      })
    });
  }
});

exports.default = _default;
module.exports = exports.default;
//# sourceMappingURL=jsonRequest.js.map